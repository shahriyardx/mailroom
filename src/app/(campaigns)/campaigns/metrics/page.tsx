/*
 * The same screen, reached without leaving the campaigns view.
 *
 * Deliverability is the thing a marketing instance watches hardest, so
 * sending it to the settings side to read its own bounce rate would be
 * sending it out of the product to use the product.
 */
export { default, dynamic } from "@/app/(mail)/settings/metrics/page";
