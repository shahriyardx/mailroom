/*
 * The same library as Settings → media, inside the campaigns shell. A
 * broadcast wants a header image as often as a template does.
 */
export { default, dynamic } from "@/app/(mail)/settings/media/page";
